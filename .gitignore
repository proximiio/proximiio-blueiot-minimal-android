# The ONLY place a real credential may live — see secrets.example.properties.
secrets.properties

local.properties
build/
.gradle/
.idea/
*.iml
.kotlin/
.DS_Store

# Release builds from Android Studio, and the daemon JDK it pins on this machine.
/app/release/
/gradle/gradle-daemon-jvm.properties
