<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The venue is downloaded once; the relay is a WebSocket. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Android freezes a backgrounded process within minutes of the last Activity
      stopping, and Doze stops network delivery with it. The SDK's answer is a foreground
      service (`ProximiioServiceOptions`, see `Venue.kt`). From API 34 the platform
      refuses `startForeground` unless the permission for the service's type is declared.
      The SDK's own manifest declares the service; these are the grants the app declares
      for it.

      `location` is the only foreground-service type a relay-only app holds: the SDK
      derives the type mask from the permissions the app holds, and a relay-only app
      scans nothing.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!--
      Required by `ProximiioServiceOptions.holdsWakeLock`. The position source is a
      socket and a socket is read on the CPU; in Doze the frame would wait for the next
      maintenance window.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      The one runtime permission this app requests (`LocationPrompt`). It is not used for
      the position: the venue's anchors locate the wristband and nothing here reads a
      coordinate off the phone. It is what lets the SDK hold the `location` service type
      above. Coarse is enough.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
      Requested with it on API 33 and above so the service's ongoing notification is
      visible. A refusal leaves the service running with the notification hidden.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      This file declares no Bluetooth permission and the app requests none at runtime.
      The phone scans nothing: the venue's BlueIoT anchors locate the wristband and
      report to the Proximi.io cloud relay, and `ProximiioConfiguration.relayOnly` turns
      the SDK's iBeacon, Eddystone and UWB sources off. The SDK's own manifest still
      contributes BLUETOOTH_SCAN and BLUETOOTH_CONNECT to the merged manifest; neither is
      ever requested.
    -->

    <application
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VenueMap">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
